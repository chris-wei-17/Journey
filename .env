# Database Configuration (placeholder for development - commented out for debugging)
# DATABASE_URL=postgresql://localhost:5432/fitjourney_dev

# Authentication
JWT_SECRET=development-secret-key-fitjourney-2025

# Node Environment
NODE_ENV=development

# Optional: Custom port for local development
PORT=5000

# Supabase Configuration (required for photos) - TEMPORARY FOR LOCAL DEBUG
SUPABASE_URL=https://fake-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=fake-service-role-key-for-local-debugging-only